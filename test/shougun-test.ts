import * as chai from "chai";

chai.should();

describe("Shougun", () => {
    describe("queryRecent", () => {
        it("sorts watched external media higher than unwatched");
        it("sorts both-watched external media based on timestamps");
    });
});
